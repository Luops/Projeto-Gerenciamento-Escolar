spring:
  application:
    name: SistemaEscolar

  datasource:
    url: jdbc:postgresql://localhost:5432/SistemaEscolar
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration